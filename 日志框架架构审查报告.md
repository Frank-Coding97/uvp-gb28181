# UVP-GB28181 日志框架架构审查报告

- 审查对象：**开源库 `develop` @ `5900149c`**
  （`server/app/utils/logging` 运行时 + `server/internal/loggingcontract` 门禁 + `server/internal/loggingacceptance` 验收）
- 审查日期：2026-09-14
- 审查视角：架构师（设计完备性 / 可运维性 / 强制力是否真实生效）

## 一句话结论

**这套日志框架的设计：合格 —— 明显高于同类项目均值，若干硬约束是"想清楚了才写得出来"的。**

第三节的问题分两层，**不要混在一起读**：

| 层 | 内容 | 说明 |
|---|---|---|
| **日志框架本体**（设计） | 运行时包、类型感知门禁、验收套件 | 这才是"设计是否合格"要回答的对象。本层有 3 处真实设计缺口，见 3.1 |
| **让它生效的机制**（工程流程） | CI 有没有在跑、调用方有没有守约、例外清单有没有人审 | **不属于日志框架设计**。它决定"这套设计今天有没有起作用"，不决定"设计好不好" |

> 口径提醒：把第二层的问题写进"设计是否合格"的结论里，是范围扩散。本报告保留第二层，因为它对项目有实际影响；但它的归属是**工程流程**，不是日志架构。

---

## 一、审查范围

本次只评开源库本身。以下两项经确认**不纳入**本次结论：

| 项 | 说明 |
|---|---|
| `custom/ren` 定制交付分支 | 客户定制项目，与本仓库分叉，不在本次评审范围 |
| Windows 平台支持 | 本次不要求 Windows 目标；`writer_fs_unix.go` 的 `//go:build linux \|\| darwin` 约束按"平台范围决策"看待，不计为缺陷 |

---

## 二、设计层面：合格，且有若干值得保留的硬约束

### 2.1 核心抽象正确

- `Runtime` 拥有全部 sink；`runtimeCore` 作为**唯一的 zap Core 收口**，业务包无法绕过。
- 所有等级判定、字段清洗、预算截断都发生在**同一个 Core**里，而不是分散在各调用点 —— 这是能真正治理日志的前提。
- 配置**不可变**（`Config.clone()`），`NoticeReload` 只提示"需重启"，避免运行期半吊子热更新。

### 2.2 有"证明式快速路径"

`fast_path.go` 的 `canWriteUnchanged()` 不是"大概没事就走快路"，而是**逐项证明规范化不会删/换/加/截任何字段**，否则退回完整 sanitizer。长度检查前置于乘法，防长度溢出。这种悲观优先的优化很少见，是对的。

### 2.3 错误对象被"脱壳"而不是格式化

`sanitize.go` 的 `summarizeError()` 只识别已知包装器（`fmt.wrapError` / `net.OpError` / `url.Error` / `os.PathError`），**绝不调用任意的 `Error()`/`Unwrap()`**（那是用户代码，可能泄密或挂死），最终只输出 `class + type + code`。

`bridge.go` 里对 slog 甚至**拒绝调用 `Value.Resolve()`**，理由写在注释里："arbitrary LogValuers may expose credentials"。这是真正的安全思维，不是抄模板。

### 2.4 日志系统自身不会成为故障源

- 三级降级：文件 sink 失败 → 限流上报（`logging.sink_failure`，每分钟一条）→ 连上报都失败 → `emergencyWriter` 直写 stderr；zap 内部错误单独计数不重复计。
- **失败日志永不缓存重放**（`rememberFailure` 注释明确 "Failed records are never retained or replayed"）—— 避免日志系统故障反噬内存。
- 文件写入全部走 `openat` + `O_NOFOLLOW` + `flock` + inode 身份校验（`matches()`），rename 用 `RENAME_NOREPLACE` / `RENAME_EXCL`，**防符号链接攻击与目录穿越**。压缩先发布完整 gzip 再删原文件，`.tmp` 残留下次启动可恢复。
- 有界性到位：`fieldBudget=24KB` / `maxFields=56` / 单字段 2KB / `EventHub` 环 2000 + 队列 256 + 丢旧策略 / `Repeater` 4096 + 超长 key 用 sha256 替代前缀（避免长前缀互相碰撞）。

### 2.5 门禁是"类型感知"的，不是 grep —— 设计上是本次最大亮点

`internal/loggingcontract` 用 `go/packages` 做真类型检查：能识别 logger 的**来源**（global / injected / local / bridge）、链式调用（`With().Named().Info()`）、构造函数变量别名、返回 `zap.Field` 的 helper、以及 `Check/CheckedEntry.Write/WrapCore` 这类逃生口。

例外机制**精确到 (包, 文件, 函数, 方法, 消息, 次数)**，不是包级 allow-list；adapter 例外还带 **SHA-256 源码封条** —— 改了源文件就自动失效要求重审。这解决了"例外清单变成永久后门"的经典问题。

`TestLoggingPolicyRepository` 是个**普通测试**（无 build tag），CI 的 `go test ./... -count=1` 会执行它，对仓库真实源码**任何** finding 直接 `t.Errorf` 失败。

---

## 三、问题清单（按层分开）

### 3.1 日志框架本体：3 处真实设计缺口

这三条属于"设计是否合格"的范围，要改的是框架或契约本身。

#### （1）双入口并存，且没有收敛路线

`app.ZapLog`（全局）与 `app.Log(ctx)`（带上下文）**同时是合法写法**。契约对前者只要求"补 `event` / 不许动态字段"，并不禁止。

结果是 42% 的调用点天然拿不到 `request_id` / `execution_id` 关联字段 —— **框架自己留了一个"绕过上下文"的正当出口**。而关联最弱的恰恰是 SIP 处理、调度任务、级联这些最难排查的链路。

设计上该做的是：把全局入口明确标为 deprecated，并在门禁里加一条"新增代码禁止使用全局 logger"的规则（契约已经有类型感知能力，加这条不难），而不是靠自觉。

#### （2）契约的强制力依赖调用点的书写形式，合理封装反而通不过

"字段必须在调用点静态可见"这个立场本身是对的。问题是框架**没有提供一条正路** —— 没有类型安全的 event 常量集，也没有被门禁认得的官方 helper。

于是 `cascade_invite_handler.go` 这种"一个 helper 收口 9 个事件分支"的正常重构，必然产出 9 条 finding（详见 3.2）。**开发者面对的选择只剩下：把字段写散在各调用点，或者去写例外。** 两种都不是好结果。

缺的是 `WithEvent(constant) *EventLogger` 这类被契约识别的官方形态。这是本次最值得改的设计点 —— 它决定了这条门禁长期是"帮手"还是"绊脚石"。

#### （3）`logs.routes` 是死配置键

`config.go:163` 解析并校验了它，但**全仓库零消费点**，也不存在任何"打印已注册路由"的实现 —— 这个开关从来没接上（`grep -rE "Config\.Routes|c\.Routes"` 只命中 config.go 自身和一个断言它为 false 的测试）。配置面存在无法兑现的键，属交付不完整：要么补实现，要么删键。

#### 附带两条（本体实现层的小问题）

- `logger.go` 的 `with()` 里 `clone.bound` 存在"构建-丢弃-重建"的冗余（先拷父 map、加本批字段、再从父 map 重建、最后补回 clean 字段）。结果正确，但每次 `With` 多两次 map 分配，逻辑也容易让后续维护者误读。
- `variable.go` 的 `DB()` 仍保留 `log.Fatal("数据库连接失败")`，`schedulerhelper/options.go` 保留 `log.Fatalf`（属已登记的 standalone 例外）。但 `log.Fatal` 会绕过 `LogRuntime.Close()`，与"关闭前必须 drain"的约束在语义上仍有张力。

#### 另两条属"已知限制但未登记"

- `EventHub` 是**单进程内存态**：多实例部署时每个实例只有自己的环，操作员在实时控制台只能看到"当前实例"。首版可接受，但应写进架构文档，而不是留给运维去发现。
- **等级变更需重启**：现场排障要把 `logs.level` 调成 debug，代价是一次重启 —— 对国标平台（SIP 注册、点播状态会断）成本不低。这条是**有意识的取舍**（`NoticeReload` 已明示需重启），只是 `zap.AtomicLevel` 本可让等级单独支持运行期切换。

---

### 3.2 工程流程：门禁当前失去执行力

以下内容回答的是"这套设计今天有没有在起作用"，**不是"设计好不好"**。

#### P0-1 CI 自 2026-08-11 起被手工停用，34 天零运行

这不是日志框架的缺陷，是**这套门禁暂时没有执行者**。事实：

```
GET /repos/Frank-Coding97/uvp-gb28181/actions/workflows
  name  : CI and Deploy Test
  path  : .github/workflows/ci-deploy-test.yml
  state : disabled_manually      ← 手工停用
  updated: 2026-08-11T01:56:03Z
```

`develop` 分支全部历史运行记录（`total_count=72`）最后一次是：

```
2026-08-10T12:58:54Z | develop | CI and Deploy Test | completed | failure | e4380b1d
```

而 GitHub 上 `develop` 的实际 head 是 `11911913`（2026-09-13），仓库 `pushed_at` = 2026-09-14T06:58:31Z（今天仍在推）。**代码在推，CI 一条都没跑。**

受影响的不只是日志门禁，是整条质量信号链：`go test ./...`、Linux 双二进制构建、前端 `pnpm run build:prod`、版本一致性检查 —— 全部 34 天没有自动执行过。

> 这也解释了为什么"CI 应为红色"的推断得不到验证：不是 CI 判绿，是 CI 根本没启动。

#### P0-2 契约门禁实红：13 项 findings，违规代码已在远程 `develop` 上

本地实测（`git diff --stat origin/develop..HEAD -- <三个文件>` 为空，即远程版本**逐字节相同**）：

```
--- FAIL: TestLoggingPolicyRepository
  app/gb28181/cascade_catalog_push.go:84  PushCatalog      missing_event
  app/gb28181/cascade_catalog_push.go:91  PushCatalog      missing_event
  app/gb28181/cascade_invite_handler.go:475 log              unresolved_logger
  app/gb28181/cascade_invite_handler.go:477 log              unresolved_logger
  app/gb28181/cascade_invite_handler.go:479 log              unresolved_logger
  app/gb28181/cascade_invite_handler.go:481 log              unresolved_logger
  app/gb28181/cascade_invite_handler.go:483 log              unresolved_logger
  app/gb28181/cascade_invite_handler.go:485 log              unresolved_logger
  app/gb28181/cascade_invite_handler.go:487 log              unresolved_logger
  app/gb28181/cascade_invite_handler.go:489 log              unresolved_logger
  app/gb28181/cascade_invite_handler.go:493 log              unresolved_logger
  app/gb28181/controllers/device_delete.go:257 deleteDeviceByID  missing_event
  app/gb28181/controllers/device_delete.go:328 deleteChannelByID missing_event
FAIL  uvp-gb28181/internal/loggingcontract  6.204s
```

**9 项 `unresolved_logger` 是同一个问题，不是 9 个 bug。**

`cascade_invite_handler.go:464` 的 `log()` 把字段攒进一个变量再展开：

```go
func (h *cascadeVideoRuntime) log(event string, platformID uint64, err error, extra ...zap.Field) {
	fields := make([]zap.Field, 0, len(extra)+2)
	fields = append(fields, extra...)
	fields = append(fields, zap.Uint64("platformId", platformID))
	...
	app.ZapLog.Warn("级联点播 ACK 无效", append(fields, zap.String("event", "cascade.video.ack_invalid"))...)
```

触发规则在 `policy.go:543-548`：遇到类型是 logging field / field-container 的 `*ast.Ident`（这里是 `fields`）且来源不可追溯，就报 `unresolved_logger`。**这不是误报** —— 一个 `[]zap.Field` 变量从函数参数一路传到调用点，静态审计确实不可能知道里面装了什么，等于给"绕开门禁塞任意字段"留了后门。契约的立场正是 3.1（2）描述的那个缺口。

顺带说一句，这个 helper 还有个已存在的小隐患：`event` 形参是 `string` 类型，所以真正被静态校验的从来不是它，而是 switch 分支里手写的字面量 —— 现在连那点校验也因 `append(...)...` 展开而失效了。

**4 项 `missing_event`** 是轻量版：`cascade_catalog_push.go:84,91`、`device_delete.go:257,328` 都有完整结构化字段（`platformId` / `deviceCode` / `retiredProjections`），唯独缺 `event`。契约对"迁移区间"内的调用容忍结构化风格，但仍要求静态 `event`。补一个 `zap.String("event", "…")` 即可，纯机械改动。

`missing_event` 描述里的 "migration-range" 指**迁移区间**，不是"可以缓办"：契约容忍风格过渡，但不豁免 `event`。

#### P1 采用率 58% / 旧写法仍占 42%（与 3.1（1）是同一件事的两面）

生产代码（已排除测试）：`app.Log(ctx)` **164** 处 vs `app.ZapLog.` **119** 处。

设计上留了双入口（3.1（1）），工程上就没有收敛的动力。P0-2 的 13 项违规只是这条结构性欠账冒出来的头。

#### P2 验收套件不在任何自动流程里

`internal/loggingacceptance/*.go` 与 `server/main_logging_acceptance.go` 都带 `//go:build logging_acceptance`，而 CI 的 `go test ./...` 不带该 tag。也就是说"panic 是否被脱敏、500 是否产出正确事件、预算截断是否生效"这类**最有价值的端到端断言**，只在人工显式加 tag 时才执行；配合 P0-1，这些断言实际上从未自动跑过。

---

## 四、值得肯定的设计取舍（建议写进架构决策记录）

1. 事件命名用 `http.access` / `cascade.video.failed` 这类**点分段**，并已支撑起前端实时控制台的 `outcome` / `stage` 派生。
2. `WithIdentity()` 绑定的 `request_id` / `execution_id` **不可被普通字段覆盖**，身份字段有真正的所有权。
3. stdout 与文件双写时**只对 stdout 过滤常规访问日志**，文件保留完整审计流 —— 区分了"给人看"和"给审计看"两种用途。
4. 重复失败聚合（`Repeater`）设计成**显式 opt-in**（`Repeater.Fail/Recovered`），普通日志不经过聚合，不会因聚合而丢时间线。
5. 启动失败在 logger 尚未建立时输出**单行 JSON 到 stderr**，且只带 `error_class` 不带原始错误文本（`ReportStartupFailure`）。
6. `main.go` 的 `finishApplication(runApplication())` 结构 + `entrypoint_test.go` 用 AST 断言"`LogRuntime.Close()` 必须早于 `os.Exit`" —— 把关闭顺序变成了可测试的约束。
7. 历史例外是**带封条的精确清单**（`reviewed_legacy.json` 92 条 + `reviewed_adapters.json` 6 文件 / 31 调用，附 sha256），而不是"某目录以后不用管了"。

---

## 五、建议动作

### 先做（属于日志框架本体的设计改进）

1. **给出官方 helper 形态**（3.1（2））：让"一个 helper 收口多个事件"这条正常路径能通过门禁，否则门禁会持续逼人写例外。这是投入产出最高的一条。
2. **给全局入口定去留**（3.1（1））：标 deprecated + 门禁加"新代码禁用全局 logger"规则，同时给出 42% 的收敛计划。
3. **`logs.routes` 补实现或删键**，并在 `deployment_test.go` 里加一条"配置项必须有消费点"的断言，防止再长出死键。
4. **技术债登记**：`with()` 的重复 map 构造、`log.Fatal` 绕过 `Close()`、`EventHub` 单进程限制、运行期等级切换。

### 再做（工程流程，与设计无关但决定设计能否起作用）

5. **重新启用 CI** —— 到仓库 Settings → Actions 把 `CI and Deploy Test` 打开。不做这步，上面几条守不住。
6. **处理 13 项 findings**：4 处补 `event`（机械），9 处消除 `fields` 变量（若第 1 条落地，这 9 项应能自然通过）。
7. **把验收套件接进 CI**：挑不依赖真 Redis/MySQL 的用例，用 `-tags logging_acceptance` 跑成独立 job。

---

## 六、复现命令

```bash
# Go 不在 PATH，需显式指定
export PATH=/opt/homebrew/Cellar/go/1.25.6/bin:$PATH
cd server

# 1) 仓库策略门禁（exit 1 = 有 findings）
go run ./internal/loggingcontract/cmd -root . -repository -fail-on-findings

# 2) 门禁测试（CI 里同样会跑，当前 FAIL，13 findings）
go test ./internal/loggingcontract -run TestLoggingPolicyRepository -count=1 -v

# 3) 框架自身测试（当前全绿，约 2 分钟）
go test ./app/utils/logging/ ./internal/... -count=1

# 4) 验收套件（默认被 build tag 挡住，需显式开启）
go test -tags logging_acceptance ./internal/loggingacceptance/ -count=1

# 5) 确认违规代码是否已在远程
git diff --stat origin/develop..HEAD -- server/app/gb28181/cascade_invite_handler.go \
  server/app/gb28181/cascade_catalog_push.go server/app/gb28181/controllers/device_delete.go   # 空 = 已推送

# 6) 采用率（设计入口是否收口）
grep -rn "app\.ZapLog\." --include="*.go" app/ bootstrap/ plugins/ | grep -v "_test.go" | wc -l   # 119
grep -rn "app\.Log("     --include="*.go" app/ bootstrap/ plugins/ | grep -v "_test.go" | wc -l   # 164

# 7) CI 是否真的在跑（公开仓库，无需 token）
curl -s "https://api.github.com/repos/Frank-Coding97/uvp-gb28181/actions/workflows" | \
  python3 -c "import sys,json;[print(w['name'],w['state'],w['updated_at']) for w in json.load(sys.stdin)['workflows']]"
# 当前输出：CI and Deploy Test disabled_manually 2026-08-11T01:56:03Z
```
