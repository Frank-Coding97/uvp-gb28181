-- 回退「媒体节点退役凭据」：整表删除。
--
-- 语义说明：本表只是"已删除节点"的撤销凭据副本，**没有**任何其它表引用它，
-- 也没有软删列需要保留历史 ⇒ 直接 `DROP TABLE` 即可（与建表带的索引一起消失）。
--
-- ⛔ 删掉它不会让已经解约成功的事实失效，只会让**还没解约完**的节点重新变成
--   "未知来源"（退回 hook 认证拒绝日志的折叠路径）。所以 down 之后如果 220 那类
--   遗留 ZLM 仍在回调，日志会重新出现 —— 这是预期行为，不是回归。

DROP TABLE IF EXISTS `meta_node_retired`;
